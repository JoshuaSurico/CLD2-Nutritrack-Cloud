<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NutriTrack Cloud</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body class="bg-slate-50 min-h-screen">
    <nav class="bg-white border-b border-slate-200 px-6 py-4 mb-8">
        <div class="max-w-6xl mx-auto flex justify-between items-center">
            <div class="flex items-center gap-2">
                <span class="text-2xl">🍽️</span>
                <span class="text-xl font-bold bg-gradient-to-r from-blue-600 to-indigo-600 bg-clip-text text-transparent">
                    NutriTrack Cloud
                </span>
            </div>
            <div class="flex gap-4 items-center">
                <% if (typeof userId !== 'undefined') { %>
                    <a href="/dashboard" class="text-slate-600 hover:text-blue-600 font-medium">Dashboard</a>
                    <a href="/profile" class="text-slate-600 hover:text-blue-600 font-medium">Mon Profil</a>
                    <a href="/logout" class="bg-slate-100 text-slate-700 px-4 py-2 rounded-lg hover:bg-red-50 hover:text-red-600 transition">Déconnexion</a>
                <% } else { %>
                    <a href="/login" class="text-slate-600 hover:text-blue-600 font-medium">Connexion</a>
                    <a href="/register" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition">S'inscrire</a>
                <% } %>
            </div>
        </div>
    </nav>
    <main class="max-w-6xl mx-auto px-6">